version: '3.8'
services:
  pg:
    image: postgres:14
    ports:
      - "5432:5432"
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - ./initdb/:/docker-entrypoint-initdb.d/
